export type AnalyticsMetrics = {
  revenue: number;
  orders: number;
  traffic: number;
  conversions: number;
};

export type ChartDataPoint = {
  date: string;
  revenue: number;
  orders: number;
  traffic: number;
};
